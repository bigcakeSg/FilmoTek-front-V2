import { css } from '@styled-system/css';

export const statsContainer = css({
  overflow: 'auto',
  height: '100%',
  padding: '30px'
});

export const statsContent = css({
  maxWidth: '1850px',
  margin: '0 auto'
});
